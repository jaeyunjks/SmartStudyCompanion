import SwiftUI

enum AIChatTheme {
    static let background = Color(uiColor: .systemGroupedBackground)
    static let accent = Color(red: 0.24, green: 0.42, blue: 0.33)
    static let accentSoft = Color(uiColor: .tertiarySystemBackground)
    static let assistantBubble = Color(uiColor: .secondarySystemBackground)
    static let userBubble = Color(uiColor: .systemBackground)
    static let surface = Color(uiColor: .secondarySystemBackground)
    static let surfaceSecondary = Color(uiColor: .tertiarySystemBackground)
    static let secondaryText = Color(.secondaryLabel)
    static let shadow = Color.black.opacity(0.10)
}
